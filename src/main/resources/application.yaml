server:
  port: 8080

spring:
  application:
    name: json_view

  datasource:
    url: jdbc:postgresql://localhost:5435/json_view_db
    username: user
    password: pass
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
